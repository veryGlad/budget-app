import React from 'react';
import Header from '../Header';

const ExpensesPage = () => {
  return (
    <div>
      <Header header={'Expenses'} />
      Expenses page
    </div>
  );
};

export default ExpensesPage;
