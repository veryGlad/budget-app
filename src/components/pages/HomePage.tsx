import React from 'react';
import Header from '../Header';

const HomePage = () => {
  return (
    <div>
      <Header header={'Home'} />
      Home page
    </div>
  );
};

export default HomePage;
